{"id":760825357,"lang":"javascript","lang_name":"JavaScript","time":"10 months, 3 weeks","timestamp":1659204308,"status":11,"status_display":"Wrong Answer","runtime":"N/A","url":"/submissions/detail/760825357/","is_pending":"Not Pending","title":"Delete and Earn","memory":"N/A","code":"/**\n * @param {number[]} nums\n * @return {number}\n */\nvar deleteAndEarn = function(nums) {   \n\n    //if you have duplicate values in nums array, if you earn one of them, you end up earning all of them. This is because you have deleted its neighbors and therefore make its remaining duplicates \"undeletable\". Hence the problem simplifies to which values can earn you the largest total.\n    \n    let map = {}\n    \n    // for (let num of nums) {\n    //     if (map[num] === undefined) {\n    //         map[num] = num\n    //     } else {\n    //         map[num] += num\n    //     }\n    // }\n    \n    for (let num of nums) {\n        if (!(num in map)) map[num] = 0\n        map[num] += num\n    }\n    \n    let prevMaxPoints = 0\n    let maxPoints = map[1] || 0\n    \n    for (let num = 2; num <= 1000; num++) {\n        const thisPoints = map[num] || 0\n        const newMaxPoints = Math.max(maxPoints, thisPoints + prevMaxPoints);\n        prevMaxPoints = maxPoints;\n        maxPoints = newMaxPoints;\n    }\n    \n    return maxPoints\n};","compare_result":"11111111111111111111111111111111111111111111110101","title_slug":"delete-and-earn","has_notes":false}