{"id":933340467,"lang":"javascript","lang_name":"JavaScript","time":"2 months, 1 week","timestamp":1681430485,"status":11,"status_display":"Wrong Answer","runtime":"N/A","url":"/submissions/detail/933340467/","is_pending":"Not Pending","title":"Find Root of N-Ary Tree","memory":"N/A","code":"/**\n * // Definition for a Node.\n * function Node(val, children) {\n *    this.val = val === undefined ? 0 : val;\n *    this.children = children === undefined ? [] : children;\n * };\n */\n\n/**\n * @param {Node[]} tree\n * @return {Node}\n */\nvar findRoot = function(tree) {\n   // To return the root of the N-ary tree, we need to find the node that doesn't have any parent. In an N-ary tree, each node can have multiple children, but only one parent.\n    \n    const childValues = new Set();\n    for (let i = 0; i < tree.length; i++) {\n        const node = tree[i];\n        if (node.children) {\n            for (let j = 0; j < node.children.length; j++) {\n                const child = node.children[j];\n                childValues.add(child);\n            }\n        }\n    }\n    for (let i = 0; i < tree.length; i++) {\n        const node = tree[i];\n        if (!childValues.has(node.val)) {\n            return node;\n        }\n    }\n    return null;\n};","compare_result":"000000100000000000000000000000011000000","title_slug":"find-root-of-n-ary-tree","has_notes":false}