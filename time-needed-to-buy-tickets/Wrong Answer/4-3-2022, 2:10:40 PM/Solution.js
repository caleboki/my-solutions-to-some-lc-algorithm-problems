// https://leetcode.com/problems/time-needed-to-buy-tickets

/**
 * @param {number[]} tickets
 * @param {number} k
 * @return {number}
 */
var timeRequiredToBuy = function(tickets, k) {
    if (k === tickets.length - 1 || k === 0) return (2*tickets.length)
    
    
    
};